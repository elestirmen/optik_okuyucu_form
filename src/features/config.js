export const MARKER_OFFSET = 8;
export const MARKER_SIZE = 26;
export const MARKER_INNER = 10;
export const SOUND_COOLDOWN_MS = 700;

export const CONFIG = {
    // Can be extended with more global config if needed
};
